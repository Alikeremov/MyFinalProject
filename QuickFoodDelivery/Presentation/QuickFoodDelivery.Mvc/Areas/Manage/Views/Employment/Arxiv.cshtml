@model ICollection<EmploymentItemVm>
@{
    int count = 1;
}


<div class="col-xl-12">
    <div class="card custom-card">
        <div class="card-header justify-content-between">
            <div class="card-title">
                Arxiv
            </div>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table text-nowrap">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col">Count</th>
                            <th scope="col">Image</th>
                            <th scope="col">Tittle</th>
                            <th scope="col">Subtittle</th>
                            <th scope="col">ButtonText</th>
                            <th scope="col">Update</th>
                            <th scope="col">Soft Delete</th>
                            <th><a asp-action="Index" asp-controller="Employment" class="btn btn-success"><i class="fa fa-address-book"></i></a></th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@(count++)</td>
                                <td>
                                    <div class="thumb_strip">
                                        <img style="width:50px; height:50px" src="~/assets/img/serviceImages/@item.Image" alt="">
                                    </div>
                                </td>
                                <td scope="row">@item.Tittle</td>
                                <td>@item.Subtittle</td>
                                <td>@item.ButtonText</td>
                                <td><a asp-action="ReverseDelete" asp-controller="Employment" asp-route-id="@item.Id" class="btn btn-info"><i class="fas fa-history"></i></a></td>
                                <td><a asp-action="Delete" asp-controller="Employment" asp-route-id="@item.Id" class="btn btn-danger"><i class="fas fa-trash-alt"></i></a></td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>